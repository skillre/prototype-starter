{
  "$schema": "./lib/factory-lock.schema.json",
  "schemaVersion": 1,
  "kind": "factory-baseline",
  "starter": {
    "packageName": "prototype-starter",
    "version": "1.2.0",
    "commit": "64d743dbdd36b75603aa39acf696aec1804169d3",
    "recordedOn": "2026-09-15"
  },
  "policy": {
    "version": "1.3.0"
  },
  "managedSurfaces": [
    "factory-policy.json",
    "factory.lock.json",
    "lib/factory-policy.schema.json",
    "lib/factory-lock.schema.json",
    "scripts/lib/agent-policy.mjs",
    "scripts/guard-agent-policy.mjs",
    ".github/workflows/ci.yml"
  ],
  "unresolved": [
    {
      "id": "upstream/control-repo",
      "what": "skillre/prototype-factory-control 是否存在、是否已发布可复用的 reusable-prototype-ci.yml@v1",
      "why": "CI 想优先调用根控制仓的 reusable workflow，但那需要一个真实存在、且打了 v1 的外部 workflow 引用。引用一个不存在的仓会让 workflow 直接不可用——CI 不是「跑得慢」，是根本不跑。",
      "probe": "2026-09-15 四次核实，全部指向「不存在」：(1) curl https://api.github.com/repos/skillre/prototype-factory-control → 404；(2) https://github.com/skillre/prototype-factory-control → 404；(3) 本地 /Users/skillre/ai-prototypes/prototype-factory-control → 不存在，且全工作区 grep 'prototype-factory-control' → 0 命中；(4) 真正的根控制面是 /Users/skillre/ai-prototypes 本身：`git remote -v` 为空、`git rev-parse HEAD` 失败（main 无任何 commit）、且没有 .github/ 目录 —— 所以 reusable-prototype-ci.yml 既不在本地也不在任何远端。",
      "checkedOn": "2026-09-15",
      "value": null
    },
    {
      "id": "upstream/control-repo-commit",
      "what": "根控制仓的 commit SHA / v1 tag 指向的确切内容",
      "why": "锁定一份不可复现的外部引用比不引用更糟：它看起来像被钉住了，实际谁也没核过。",
      "probe": "未执行——没有可解析的 ref：远端仓不存在，根控制面仓 main 无 commit、无 remote。宁缺勿猜。",
      "checkedOn": "2026-09-15",
      "value": null
    },
    {
      "id": "upstream/starter-remote",
      "what": "本仓 origin（github.com/skillre/prototype-starter）是否已发布，CI 是否真的有运行场所",
      "why": "`.github/workflows/ci.yml` 只有在仓被推到一个真实远端之后才会运行。把「CI 会跑」当既成事实，会在第一次 push 时才发现它从来没有运行过。",
      "probe": "curl https://api.github.com/repos/skillre/prototype-starter → 404（本仓 git remote -v 指向该地址）。观察于 2026-09-15；未执行 push，因此未做进一步核实。",
      "checkedOn": "2026-09-15",
      "value": null
    }
  ],
  "note": "baseline 自身的治理锁，记录升级到 policy 1.3.0 那一刻的事实。`starter.commit` 是升级开始时基线 HEAD 的 SHA，不是「当前 HEAD」——HEAD 前进不会让锁失效（校验器只在漂移时给 warning，不判失败）。`AGENTS.md` 里只有 `factory-core-policy` 管理块受管，块外是人类写的文档。已用本仓校验器逐字比对根控制面 contracts/factory-lock.schema.json：本锁命中 oneOf 的「Factory baseline governance lock」分支（108 条断言全过），`starter.version` 1.2.0 落在 catalog/compatibility.json 的 supportedVersionRange `>=1.1.0 <2.0.0` 内。控制面的 catalog/projects/starter.json 仍记 `lockPresent: false`、`lockPath: factory/factory.lock.json`——那是升级前的扫描快照，需由控制面一侧刷新（不在本仓范围）。"
}
